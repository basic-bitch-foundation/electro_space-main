[build]
  # go into the frontend folder before running anything
  base    = "artifacts/electrospace"
  command = "npm install && npm run build"
  publish = "dist/public"

[build.environment]
  # import.meta.dirname needs node 20.11+
  NODE_VERSION = "20"